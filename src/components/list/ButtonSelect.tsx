import { selectedItem } from "../../interfaces/buttonSelected"
import "./List.scss"

export const ButtonSelect = ({ selected, header }: { selected: selectedItem, header: boolean }) => {

    return (
        <button className="selectButton" >
            caca
        </button>
    )
}
